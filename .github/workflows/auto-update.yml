---
name: Auto-Update Dependencies

on: # yamllint disable-line rule:truthy
  schedule:
    # Run weekly on Monday at 02:00 UTC
    - cron: '0 2 * * 1'
  workflow_dispatch:
    inputs:
      force:
        description: 'Force update check even if no updates available'
        required: false
        type: boolean
        default: false

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  auto-update:
    uses: miikkak/workflows/.github/workflows/auto-update.yml@43ce0c3b01a799abd154c00c009dc7b2d8160d3c # main
    secrets: inherit
    with:
      force: ${{ inputs.force || false }}
